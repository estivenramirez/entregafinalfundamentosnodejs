    <header>
      <!-- Fixed navbar -->
      <nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">
        <a class="navbar-brand" href="/">{{nombreAplicacion}}</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarCollapse">
          <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
              <a class="nav-link" href="/">Inicio<span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="/cursos/ver">Ver Cursos</a>
            </li>
            {{#if (or (eq 'COORDINADOR' se_rol) (eq 'ADMINISTRADOR' se_rol)) }}
            <li class="nav-item">
              <a class="nav-link" href="/cursos/crear">Crear Curso</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/cursos/verInscritos">Ver Inscritos</a>
            </li>
            {{/if}}
            {{#if (eq 'ADMINISTRADOR' se_rol) }}
            <li class="nav-item">
                <a class="nav-link" href="/usuarios/ver">Ver Usuarios</a>
            </li>
            {{/if}}
            {{#if (eq 'ASPIRANTE' se_rol) }}
            <li class="nav-item">
              <a class="nav-link" href="/cursos/inscribir">Inscribir Curso</a>
            </li>
            {{/if}}
            {{#if (ne se_sesion true)}}
            <li class="nav-item">
              <a class="nav-link" href="/usuarios/registrar">Registrar Usuario</a>
            </li>
            {{/if}}
          </ul>
            {{#if se_sesion}}
            <form class="form-inline mt-2 mt-md-0" action="/salir" method="GET">
              <h6 class="mr-1 text-light">{{se_nombre}} <small>({{se_rol}})</small></h6>
              <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Salir</button>
            </form>
            {{else}}
            <form class="form-inline mt-2 mt-md-0" action="/ingresar" method="POST">
              <input class="form-control mr-sm-2" type="text" name="usuario" placeholder="Usuario" aria-label="Search">
              <input class="form-control mr-sm-2" type="password"  name="password" placeholder="Contraseña" aria-label="Search">
              <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Login</button>
            </form>
            {{/if}}
        </div>
      </nav>
    </header>
    
